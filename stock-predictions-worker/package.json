{
  "name": "stock-predictions-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "dev": "wrangler dev --local",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.10",
    "vitest": "~3.2.0",
    "wrangler": "^4.63.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
